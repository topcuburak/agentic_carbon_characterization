#!/bin/bash
#PBS -l ngpus=1
#PBS -l ncpus=8
#PBS -l walltime=12:00:00
#PBS -q workq@e5-cse-cbgpu01.eecscl.psu.edu
#PBS -N vllm-serve
#PBS -M bvt5283@psu.edu
#PBS -m bea

# ─── vLLM Model Server ───────────────────────────────────────────────────────
# Serves a model via OpenAI-compatible API on port 8000.
#
# Usage:
#   qsub scripts/serve_vllm.pbs
#   MODEL=Qwen/Qwen2.5-0.5B-Instruct qsub scripts/serve_vllm.pbs
# ──────────────────────────────────────────────────────────────────────────────

eval "$(/usr/local/anaconda3/bin/conda shell.bash hook)"
conda activate agentic-carbon

# Set HF_TOKEN in your shell before submitting, or use: qsub -V scripts/serve_vllm.pbs
export HF_TOKEN="${HF_TOKEN:?Set HF_TOKEN before submitting}"
export HF_HOME=/scratch/bvt5283/.cache/huggingface
export HF_DATASETS_CACHE=/scratch/bvt5283/.cache/huggingface/datasets

MODEL="${MODEL:-meta-llama/Llama-3.1-8B-Instruct}"
TP="${TP:-1}"
PORT="${PORT:-8000}"

echo "=== Starting vLLM server ==="
echo "Model: $MODEL"
echo "Tensor Parallel: $TP"
echo "Port: $PORT"
echo "Node: $(hostname)"
echo "Time: $(date)"

python -m vllm.entrypoints.openai.api_server \
    --model "$MODEL" \
    --tensor-parallel-size "$TP" \
    --port "$PORT" \
    --api-key "EMPTY" \
    --max-model-len 8192 \
    --enable-auto-tool-choice \
    --tool-call-parser hermes
